using FeTo.SOArchitecture;

public class ManualGameEventListener : GameEventListener
{
    protected new void OnEnable() {
        //Event.RegisterListener(this);
    }

    protected new void OnDisable() {
        //Event.UnregisterListener(this);
    }
}
